conda create -y -n splicevo python=3.10
conda activate splicevo
conda install -y numpy pandas matplotlib scipy
conda install -y -c conda-forge jupyterlab cudatoolkit-dev tensorboard
conda install -y -c bioconda gffutils
pip install torch ninja
# pip install flash-attn --no-build-isolation # failed on helix
pip install gReLU
pip install tangermeme
